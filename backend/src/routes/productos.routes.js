// routes/productos.routes.js
// Define los endpoints HTTP del recurso Productos y los conecta con su controller.
// Ejemplo de endpoints esperados:
//   GET  /api/productos?codigo_barras=...   (para el escaneo del POS)
//   POST /api/productos                     (solo Admin)
//   PUT  /api/productos/:id                 (solo Admin)
