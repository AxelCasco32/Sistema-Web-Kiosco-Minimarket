// controllers/usuarios.controller.js
// Lógica de negocio para el recurso Usuarios.
// Debe manejar: login (validar usuario/contraseña, generar JWT), alta de usuario
// (hashear contraseña antes de guardar), listado de usuarios (nunca devolver el hash),
// baja lógica (activo = false).
// Solo Admin puede crear/listar/dar de baja usuarios.
