// utils/calcularVuelto.js
// Función auxiliar: recibe el monto recibido y el total de la venta,
// devuelve el vuelto a entregar. Usada por ventas.controller.js.
// Debe manejar el caso "paga exacto" (vuelto = 0) y validar que el monto
// recibido no sea menor al total.
